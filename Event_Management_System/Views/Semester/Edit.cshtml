@model Event_Management_System.Models.Semester_Tbl
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
@using (Html.BeginForm("UpdateSemester", "Semester", FormMethod.Post))
{
    @Html.HiddenFor(x => x.Sem_id)
    <div class="row">
        <div class="col-md-6">
            <label>Semester</label>
            @Html.TextBoxFor(x => x.Sem_Name, new { @class = "form-control" })
        </div>
        @*<div class="col-md-6">
            <label>Created Date</label>
            @Html.TextBoxFor(x => x.Created_Date, new { @class = "form-control", @type = "date" })
        </div>*@
    </div>

    <div class="row">
        @*<div class="col-md-7">
            <label>Created By</label>
            @Html.TextBoxFor(x => x.Created_by, new { @class = "form-control", @type = "number" })
        </div>*@
        <div class="col-md-5">
            <input type="submit" value="Update" class="btn btn-success mt-3" />
            <a href="/Semester/Index" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
}
